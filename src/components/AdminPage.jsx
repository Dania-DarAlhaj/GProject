// src/pages/AdminPage.jsx
import React from "react";

export default function AdminPage() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "#FAF8F5",
        fontFamily: "Lato, sans-serif",
      }}
    >
      <h1 style={{ fontSize: "2rem", color: "#C9A27C" }}>
        Welcome to the admin page
      </h1>
    </div>
  );
}
